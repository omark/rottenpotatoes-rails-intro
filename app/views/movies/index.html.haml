-#  This file is app/views/movies/index.html.haml
%h1 All Movies

%table#movies
  %thead
    %tr
      - if flash[:sorted] == 'title'
        %th.hilite#title_header= link_to 'Movie Title', sort_movie_path('title')
      - else
        %th#title_header= link_to 'Movie Title', sort_movie_path('title')
      %th Rating
      - if flash[:sorted] == 'release_date'
        %th.hilite#release_date_header= link_to 'Release Date', sort_movie_path('release_date')
      - else
        %th#release_date_header= link_to 'Release Date', sort_movie_path('release_date')
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
